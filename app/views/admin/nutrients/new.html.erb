<div class="container">
  <h4 class="my-3 text-center" style="background-color: #ffb830">栄養素情報登録</h4>
  <div class="row">
    <div class="col-lg-8 col-md-10 colsm-12 px-5 px-sm-0 mx-auto">

      <!--#新規投稿エラーメッセージの表示-->
      <div class="form-body p-4">
        <% if @nutrient.errors.any? %>
        <%= @nutrient.errors.count %>エラーにより、保存できませんでした。
        <ul>
          <% @nutrient.errors.full_messages.each do |message| %>
            <li><%= message %></li>
          <% end %>
        </ul>
        <% end %>
      </div>

      <%= render "admin/nutrients/form", itme: @nutrient, url: admin_nutrients_path(@nutrient), submit: '　新規登録　' %>
    </div>
  </div>
</div>
