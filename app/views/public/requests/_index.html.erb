<h4 class="my-3 text-center">皆様からのご意見・ご要望</h4>
  <table class="my-2">
    <tbody>
      <% @requests.each do |request| %>
        <tr>
          <td><%= request.created_at.strftime('%Y/%m/%d') %></td>
        </tr>
        <tr><td><%= request.content %></td></tr>
        <tr><td class="text-right"><%= link_to "詳細", request_path(request) %></td></tr>

      <% end %>
    </tbody>
  </table>
