<div class="container">
  <h4 class="my-3 text-center">自分の体調に合った野菜を探そう◆効能一覧</h4>
  <div class="row">
    <div class="col-10">
      <div class="d-flex flex-wrap align-center">
        <% @effects.each do |effect| %>
        <div class="mr-4 d-flex flex-column mb-4">
          <%= link_to effect.name, effect_path(effect), class:"btn btn-outline-info btn-lg" %>
        </div>
        <% end %>
      </div>
    </div>
  </div>
</div>
